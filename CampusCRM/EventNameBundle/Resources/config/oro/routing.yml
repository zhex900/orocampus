oro_eventname_bundle:
    resource:     "@EventNameBundle/Controller"
    type:         annotation
    prefix:       /eventname

oro_eventname_api_account:
    resource:     "@EventNameBundle/Controller/Api/Rest/EventNameController.php"
    type:         rest
    prefix:       api/rest/{version}/
    requirements:
        version:  latest|v1
    defaults:
        version:  latest

