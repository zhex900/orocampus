{% import 'OroUIBundle::macros.html.twig' as UI %}
{% import 'OroEntityConfigBundle::macros.html.twig' as entityConfig %}

<div class="widget-content">
    <div class="row-fluid form-horizontal">
        {% if  resource_granted('VIEW', eventtopics, 'name') %}
            <div class="responsive-block">
                {{ UI.renderProperty('oro.eventtopics.name.label'|trans, eventtopics.name) }}
            </div>
        {% endif %}
        {#
        {% if  resource_granted('VIEW', eventtopics, 'system_calendar') %}
            <div class="responsive-block">
                {{ UI.renderProperty('campuscrm.eventtopics.system_calendar.label'|trans, eventtopics.systemCalendar ? 'Yes' : 'No') }}
            </div>
        {% endif %}#}
    </div>
</div>
