{% extends 'OroUIBundle:actions:index.html.twig' %}
{% import 'OroUIBundle::macros.html.twig' as UI %}
{% set gridName = 'eventtopics-grid' %}
{% set pageTitle = 'oro.eventtopics.entity_plural_label'|trans %}

{% block navButtons %}
    {% include 'OroImportExportBundle:ImportExport:buttons.html.twig' with {
    entity_class: entity_class,
    exportProcessor: 'eventtopics',
    exportTemplateProcessor: 'eventtopics',
    importProcessor: 'eventtopics.add_or_replace',
    importTitle: 'oro.importexport.import.entity'|trans({'%entityName%': 'oro.eventtopics.entity_plural_label'|trans}),
    datagridName: gridName
    } %}

    {% if resource_granted('oro_eventtopics_create') %}
        <div class="btn-group">
            {{ UI.addButton({
                'path' : path('oro_eventtopics_create'),
                'entity_label': 'oro.eventtopics.entity_label'|trans
            }) }}
        </div>
    {% endif %}
{% endblock %}
