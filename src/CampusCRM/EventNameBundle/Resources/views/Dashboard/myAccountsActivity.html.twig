{% extends 'OroDashboardBundle:Dashboard:grid.html.twig' %}
{% set gridName = 'dashboard-my-accounts-activity-grid' %}

{% block content %}
    {{ dataGrid.renderGrid(gridName, params|default({}), {
        'routerEnabled': false,
        'enableFilters': false,
    }|merge(renderParams|default({}))) }}
{% endblock %}

{% block actions %}
    {% set actions = [{
    'url': path('oro_eventname_index'),
    'type': 'link',
    'label': 'oro.dashboard.my_eventnames_activity.view_all'|trans
    }] %}

    {{ parent() }}
{% endblock %}
